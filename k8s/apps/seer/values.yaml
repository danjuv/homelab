ingress:
  enabled: true
  ingressClassName: nginx
  hosts:
    - host: seer.home.lab
      paths:
        - path: /
          pathType: Prefix
config:
  persistence:
    size: 3Gi
    storageClass: longhorn
    accessModes:
      - ReadWriteOnce

