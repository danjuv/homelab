ingress:
  enabled: true
  ingressClassName: nginx
  hosts:
    - host: overseerr.home.lab
      paths:
        - path: /
          pathType: Prefix
config:
  persistence:
    size: 3Gi
    storageClass: longhorn
    accessModes:
      - ReadWriteOnce

